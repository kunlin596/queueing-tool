#!/bin/bash
# Legacy shim: the tool now lives in the queueing_tool Python package
# (pip/uv-installable). This keeps old PATH entries at queue/ working.
REPO="$(dirname "$(readlink -f "$0")")/.."
PYTHONPATH="$REPO${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m queueing_tool.qinfo "$@"
